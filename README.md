Run the following to use the next js project template

```
git clone https://github.com/migeles/projects-template.git
```

then run:

```
npm install
```

to deattach from git run:

```
Remove-Item -Recurse -Force .git
```
